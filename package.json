{
  "name": "stocks-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker backend for the stocks app — proxies SnapTrade requests with Firebase-authenticated calls. Holds SnapTrade clientSecret as a Worker secret so it never reaches the browser.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
